apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helloworld
  namespace: demo
spec:
  chart:
    spec:
      version: ">=0.1.0"
  test:
    enable: false
  values:
    replicaCount: 3
    image: "smana/helloworld:staging-cd2c525-1624897137" # {"$imagepolicy": "flux-system:helloworld"}
