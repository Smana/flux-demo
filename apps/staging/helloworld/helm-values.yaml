apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helloworld
  namespace: demo
spec:
  chart:
    spec:
      version: ">=0.1.0"
  test:
    enable: false
  values:
    replicaCount: 3
    image: "smana/helloworld:staging-ba10491-1624890577" # {"$imagepolicy": "flux-system:helloworld"}
