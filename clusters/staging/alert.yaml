apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  name: apps
spec:
  providerRef:
    name: flux-system
  eventSeverity: info
  eventSources:
    - kind: Kustomization
      name: apps